# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# IDEs
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Generated platform folders are recreated with `flutter create .`
# (kept out of source control here because they are machine-generated).
# Remove the lines below if you want to commit them.
android/
ios/
web/
windows/
macos/
linux/

# Claude Code — keep .claude/ committed, but not personal overrides
.claude/settings.local.json
