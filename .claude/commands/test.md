---
description: Run the Flutter test suite
allowed-tools: Bash(flutter test:*)
---
Run `flutter test`. Summarize any failures with file:line and the most likely fix. If all pass, say so in one line.
